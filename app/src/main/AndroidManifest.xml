<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.smartmashroom">

    <!-- ✅ Izin yang dibutuhkan aplikasi -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- ✅ Izin proteksi internal -->
    <permission
        android:name="com.example.smartmashroom.AUTH_INTERNAL_ACCESS_ONLY"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartMushroom">

        <!-- ✅ Lindungi service Google agar tidak diekspor -->
        <service
            android:name="com.google.android.gms.auth.api.signin.RevocationBoundService"
            android:exported="false"
            tools:replace="android:exported" />

        <!-- ✅ Lindungi receiver dari AndroidX agar tidak diekspor -->
        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:exported="false"
            tools:replace="android:exported" />

        <!-- ✅ Hapus RecaptchaActivity dari Firebase -->
        <activity
            android:name="com.google.firebase.auth.internal.RecaptchaActivity"
            tools:node="remove" />

        <!-- ✅ Lindungi GenericIdpActivity -->
        <activity
            android:name="com.google.firebase.auth.internal.GenericIdpActivity"
            android:exported="true"
            android:permission="com.example.smartmashroom.AUTH_INTERNAL_ACCESS_ONLY" />

        <!-- ✅ Launcher utama -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ Activity lainnya tidak diekspor -->
        <activity android:name=".LoginActivity" android:exported="false" />
        <activity android:name=".ResetPasswordActivity" android:exported="false" />
        <activity android:name=".BerandaActivity" android:exported="false" />
        <activity android:name=".ProfilActivity" android:exported="false" />
        <activity android:name=".EditNamaActivity" android:exported="false" />
        <activity android:name=".EditPasswordActivity" android:exported="false" />
        <activity android:name=".PengembunanActivity" android:exported="false" />
        <activity android:name=".NotifikasiActivity" android:exported="false" />
        <activity android:name=".LihatEmailActivity" android:exported="false" />

    </application>
</manifest>
